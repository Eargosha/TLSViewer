==== FLUSK бек ИЛИ Flask-SocketIO
==== JS HTML CSS Bootsrap фронт
1. Както тянуть tshark.exe
2. Перед запуском mitmproxy нужно установить сертификат mitmproxy-ca-cert.pem
3. Сделать проверку PING перед использованием кода
4. Сделать функцию фильтров так же как и на Портале в суду, чтобы она обращалась к беку постоянно
а тот уже возвращал отфильтрованные данные, ну и тут будет оправдан WebSocket, ибо можно было
использовать Server-Sent Events (SSE)
!!!! По поводу мониторинга и анализа можно понаворовать фичи с PortMaster


WebSocket — это «труба» между фронтендом и бэкендом, по которой данные текут в реальном времени. Для твоего TLS-мониторинга это идеально:
    Консольное приложение → пишет пакеты в лог.
    Бэкенд (Flask-SocketIO) → читает лог и шлёт данные в браузер.
    Фронтенд → отображает пакеты мгновенно.
Попробуй минимальный пример — и ты сразу увидишь магию live-данных! 🚀


1. Расширенный анализ данных
Для анализа TLS-пакетов можно использовать следующие критерии:

a. Основные метрики TLS
TLS Version : Версия протокола (TLS 1.2, TLS 1.3).
Cipher Suite : Используемый набор шифров.
Handshake Type : Тип handshake-сообщения (Client Hello, Server Hello, Certificate, Finished и т.д.).
SNI (Server Name Indication) : Доменное имя сервера.
Session Resumption : Проверка, используется ли возобновление сессии (через Session ID или Session Tickets).
b. Статистический анализ
Количество подключений : Количество уникальных TLS-подключений к целевому серверу.
Распределение версий TLS : Процент использования различных версий TLS (например, TLS 1.2 vs TLS 1.3).
Частота запросов : Количество запросов в единицу времени (например, запросы в секунду).
Размер пакетов : Средний размер TLS-пакетов, минимальный и максимальный размер.
c. Аномалии и потенциальные угрозы
Необычные Cipher Suites : Обнаружение использования устаревших или небезопасных наборов шифров.
Сбои Handshake : Анализ случаев, когда handshake завершается неудачно.
Подозрительные домены : Выявление SNI, которые указывают на подозрительные или неизвестные домены.
Аномальная частота запросов : Обнаружение DDoS-атак или сканирования.

a. Графики
График распределения версий TLS (круговая диаграмма или столбчатая диаграмма).
График частоты запросов (временная линия, показывающая количество запросов в секунду).
График размера пакетов (гистограмма, показывающая распределение размеров пакетов).
b. Интерактивные элементы
Таблица с деталями пакетов : Отображение основных параметров каждого TLS-пакета (например, SNI, Cipher Suite, размер пакета).
Фильтры : Возможность фильтрации пакетов по различным критериям (например, по SNI, версии TLS или типу handshake).
Выделение аномалий : Подсветка подозрительных пакетов или событий (например, неудачные handshake или использование устаревших шифров).


Геолокация IP-адресов : Используйте API (например, ipstack) для определения местоположения источника и назначения.
Мониторинг HTTP/2 : Если трафик использует HTTP/2, анализируйте заголовки и тела запросов.
Экспорт данных : Реализуйте возможность экспорта данных в CSV или PDF для дальнейшего анализа.